{
  "targets": { "swap": {} },
  "passes": [
    {
      "name": "server:tint",
      "inputs": ["main"],
      "output": "swap",
      "uniforms": [{ "name": "Color", "values": [0.6, 1.0, 0.6, 1.0] }]
    },
    { "name": "blit", "inputs": ["swap"], "output": "main" }
  ]
}
